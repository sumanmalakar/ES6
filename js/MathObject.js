const num1 = 100;
const num2 = 60;
let val;

// Simple math with numbers
val = num1 + num2
val = num1 - num2
val = num1 / num2
val = num1 * num2
val = num1 % num2 // moduls operator give as reminder

// Math Object
val = Math.PI;
val = Math.E
val = Math.round(2.5);
val = Math.ceil(2.1) // It's give us Above 2
val = Math.floor(2.6) // It's give us Below 2.
val = Math.sqrt(64); 
val = Math.abs(-3);
val = Math.pow(8, 2) // power
val = Math.min(2,3,454,2,-2,54)
val = Math.max(2, 3, 454, 2, -2, 54)
val = Math.random();

val = Math.floor(Math.random() * 20 + 1);


console.log(val);